export class Ingredient {
    // public in front of parameters declared AND assigns them automatically in the constructor as a shortcut
    constructor(public name: string, public amount: number) { }
}
